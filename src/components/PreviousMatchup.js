import React from 'react'
import {withRouter} from 'react-router-dom'

const PreviousMatchup = props => {
    return (
        <div>

        </div>
    )
}

export default withRouter(PreviousMatchup)